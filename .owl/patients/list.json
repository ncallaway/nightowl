{
  "url": "{{ env.host }}/patients?token={{ env.token }}",
  "description": "list all patients",
  "method": "post",
  "parameters": [
    {
      "name": "q",
      "value": "doe"
    },
    {
      "name": "first",
      "value": "john",
      "disabled": true
    }
  ],
  "body": {
    "json": {
      "something": "{{ state.jwt }}"
    }
  }
}
